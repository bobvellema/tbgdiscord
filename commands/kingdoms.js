const discord = require("discord.js");

module.exports.run = async(bot, message, args) => {

    if (command === `${prefix}kingdoms`) 

    var embedalgemeen = new discord.RichEmbed()
    .setDescription("kingdoms")
    .setColor("#46a7f2")
    .addField("Kingdoms zijn", "Lorum")
    .setTimestamp()
    .setFooter("kingdoms", bot.user.displayAvatarURL);
    
}

module.exports.help = {
    name: "kingdoms"
}
